version: '3.7'

services:

  

  
  peer0.farmers.example.com:
    container_name: peer0.farmers.example.com
    image: hyperledger/fabric-peer:latest
    cap_add:
     - NET_ADMIN
     - SYS_ADMIN
    labels:
      service: hyperledger/fabric
    environment:
      # Generic peer variables
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=fabric_test
    volumes:
      - ./docker/peercfg:/etc/hyperledger/peercfg
      - ${DOCKER_SOCK}:/host/var/run/docker.sock
      
  

  
  peer0.aggregators.example.com:
    container_name: peer0.aggregators.example.com
    image: hyperledger/fabric-peer:latest
    cap_add:
     - NET_ADMIN
     - SYS_ADMIN
    labels:
      service: hyperledger/fabric
    environment:
      # Generic peer variables
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=fabric_test
    volumes:
      - ./docker/peercfg:/etc/hyperledger/peercfg
      - ${DOCKER_SOCK}:/host/var/run/docker.sock
      
  

  
  peer0.retailers.example.com:
    container_name: peer0.retailers.example.com
    image: hyperledger/fabric-peer:latest
    cap_add:
     - NET_ADMIN
     - SYS_ADMIN
    labels:
      service: hyperledger/fabric
    environment:
      # Generic peer variables
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=fabric_test
    volumes:
      - ./docker/peercfg:/etc/hyperledger/peercfg
      - ${DOCKER_SOCK}:/host/var/run/docker.sock
      
  

  
  peer0.consumers.example.com:
    container_name: peer0.consumers.example.com
    image: hyperledger/fabric-peer:latest
    cap_add:
     - NET_ADMIN
     - SYS_ADMIN
    labels:
      service: hyperledger/fabric
    environment:
      # Generic peer variables
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=fabric_test
    volumes:
      - ./docker/peercfg:/etc/hyperledger/peercfg
      - ${DOCKER_SOCK}:/host/var/run/docker.sock
      
  

  
  peer0.bank.example.com:
    container_name: peer0.bank.example.com
    image: hyperledger/fabric-peer:latest
    cap_add:
     - NET_ADMIN
     - SYS_ADMIN
    labels:
      service: hyperledger/fabric
    environment:
      # Generic peer variables
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=fabric_test
    volumes:
      - ./docker/peercfg:/etc/hyperledger/peercfg
      - ${DOCKER_SOCK}:/host/var/run/docker.sock
      
